# Implementation Plans

Generated by the improve skill on 2026-08-29. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

**Planned at commit:** `16ce3a5`  
**Selection note:** User unavailable — default top 5 by leverage from standard audit.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix Vitest vendored skill test pollution | P1 | S | — | DONE |
| 002 | Add Markdown Preview characterization tests | P1 | M | 001 | TODO |
| 003 | Unify and tighten DOMPurify sanitize policy | P1 | S | 002 | TODO |
| 004 | Add CI test and lint gates | P2 | S | 001 | TODO |
| 005 | Extract MarkdownPreview module from script.js | P2 | L | 002, 003 | TODO |

## Dependency notes

- **002** requires **001** so `npm test -- --run` exits 0 before adding tests.
- **003** should land after **002** so sanitize policy changes have regression tests.
- **005** requires **002** (characterization tests) and **003** (stable sanitize seam) before moving render logic out of `script.js`.
- **004** can run in parallel with **002** once **001** is done; it does not block modularization.

## Findings considered and rejected

- **Upgrade all 29 npm audit advisories immediately:** Most are dev-only (vitest/ws/transitive). Not worth a forced `--force` upgrade without a dedicated vitest bump plan; track separately.
- **Delete 704 vendored skills to shrink repo:** Runtime unaffected; symlinks are intentional per AGENTS.md. Skill bloat is agent DX, not app maintainability — document in AGENTS.md instead of mass deletion.
- **Rewrite baseline tests as integration tests:** Baseline file is intentionally a lightweight contract; expand via **002** instead of rewriting 98 placeholder assertions in one pass.
- **Refactor setupEditor before MarkdownPreview seam (legacy plan Phase 2 first):** Wrong order — without preview characterization tests, 1217-line extraction has no test surface at the new seams.

## Architecture review (read-only)

HTML report: `/tmp/architecture-review-20260829-143243.html`  
Top architecture candidate aligns with **005** (deepen Markdown Preview module).
